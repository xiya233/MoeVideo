listen              1935;
max_connections     1000;
daemon              off;
srs_log_tank        console;

http_server {
    enabled on;
    listen 8080;
    # Keep HTTP static root aligned with hls_path, otherwise m3u8/ts may be generated but always 404.
    dir /data/live-recordings;
}

http_api {
    enabled on;
    listen 1985;
}

vhost __defaultVhost__ {
    hls {
        enabled on;
        hls_path /data/live-recordings;
        hls_fragment __SRS_HLS_FRAGMENT_SEC__;
        hls_window __SRS_HLS_WINDOW_SEC__;
    }

    http_remux {
        enabled on;
        mount [app]/[stream].m3u8;
    }

    dvr {
        enabled on;
        dvr_apply all;
        dvr_path /data/live-recordings/[app]/[stream]/[timestamp].flv;
        dvr_plan session;
        dvr_wait_keyframe on;
    }

    http_hooks {
        enabled on;
        on_publish   __SRS_CALLBACK_URL__;
        on_unpublish __SRS_CALLBACK_URL__;
        on_dvr       __SRS_CALLBACK_URL__;
    }
}
